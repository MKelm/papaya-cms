"On" to use compression when delivering cached web pages to the browser, otherwise "Off". Web pages are delivered in compressed form to the browser only if this is supported by the requesting browser.