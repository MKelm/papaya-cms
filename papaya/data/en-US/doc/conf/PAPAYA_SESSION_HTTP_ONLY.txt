"On“ to allow the session cookies to be accessed exclusively over the HTTP protocol, otherwise "Off". The HTTP restriction prevents session cookies from being accessed by scripting languages like JavaScript. When this setting is active, most identity thefts via cross site scripting can be prevented. Since not all browsers support this function, an absolute safety cannot be granted.