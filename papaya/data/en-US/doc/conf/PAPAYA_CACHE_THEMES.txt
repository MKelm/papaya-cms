"On" to cache the CSS and JavaScript files in the theme directory in one CSS file and one JavaScript file respectively, otherwise "Off". CSS files in the theme directory will be combined into one CSS file by papaya CMS in order to reduce the number of HTTP requests. The same procedure is applied to all JavaScript files. In order to prevent papaya CMS from composing the CSS and JavaScript files in each page request, the combined files can be cached in the file system.