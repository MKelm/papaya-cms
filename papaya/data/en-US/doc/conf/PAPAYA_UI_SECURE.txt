<ul><li><em>on</em>: The administration backend can only be accessed via https.<br /></li><li><em>off</em>: You can access the administration backend of papaya CMS both via the standard http connection and via https (default).<br /></li></ul>You can select "On" only when you are already logged in via https. It is also possible to enforce a similar behavior for frontend pages. Sessions in the frontend will then only be opened for connections over HTTPS, see <tt>PAPAYA_SESSION_SECURE</tt>.