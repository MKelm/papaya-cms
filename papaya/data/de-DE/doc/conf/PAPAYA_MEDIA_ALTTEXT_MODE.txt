Bestimmt, welcher Text für das alt-Attribut von Bildern in der MediaDB genommen wird:<ul><li><em>explicit only</em>: Das <code>alt</code>-Attribut wird nur ausgegeben, wenn das <code>alt</code>-Attribut explizit angegeben worden ist.<br /></li><li><em>description</em>: Der gekürzte Text aus dem Feld „Beschreibung“ in den Bildeigenschaften wird in das <code>alt</code>-Attribut eingefügt.<br /></li></ul>