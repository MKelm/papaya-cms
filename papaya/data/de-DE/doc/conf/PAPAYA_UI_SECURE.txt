<ul><li><em>on</em>: Die Admin-Oberfläche kann nur über https aufgerufen werden.<br /></li><li><em>off</em>: Die Admin-Oberfläche kann sowohl über eine normale http-Verbindung als auch über https aufgerufen werden (Standardeinstellung)<br /></li></ul>Sie können die Einstellung „on“ nur dann setzen, wenn Sie sich bereits über eine https-Verbindung im Backend angemeldet haben. Sie können erzwingen, dass Sessions auch über das Frontend nur über das HTTPS-Protokoll geöffnet werden können, siehe <tt>PAPAYA_SESSION_SECURE</tt>.